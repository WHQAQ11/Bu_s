# 阶段2: 占卜功能迁移修改清单

## 📋 总体目标
将占卜核心功能迁移到Supabase架构，实现数据持久化和AI解读功能。

## 🔧 技术架构变更
- **数据存储**: 从localStorage → Supabase数据库
- **占卜计算**: 前端保留基础算法，后端增强功能
- **AI解读**: 外部API → Supabase边缘函数
- **历史记录**: 新增完整的历史记录系统

## 🗃️ 数据库设计更新

### 1. 卦象数据表 (hexagrams)
```sql
CREATE TABLE hexagrams (
  id SERIAL PRIMARY KEY,
  hexagram_key TEXT UNIQUE NOT NULL,     -- 6位二进制字符串
  name TEXT NOT NULL,
  pinyin TEXT,
  number INTEGER NOT NULL,
  guaci_original TEXT,
  guaci_translation TEXT,
  guaci_interpretation TEXT,
  yaoci JSONB,                           -- 爻辞数组
  structure JSONB,                       -- 卦象结构
  source JSONB,                          -- 出处信息
  references JSONB,                      -- 古籍引用
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2. 占卜记录表优化
```sql
-- 已存在，需要验证结构完整性
divination_logs (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  method TEXT NOT NULL CHECK (method IN ('liuyao', 'meihua', 'ai')),
  question TEXT NOT NULL,
  category TEXT,
  original_hexagram TEXT NOT NULL,
  transformed_hexagram TEXT,
  changing_indexes INTEGER[],
  ben_gua_info JSONB,
  bian_gua_info JSONB,
  ai_interpretation TEXT,
  interpretation_status TEXT DEFAULT 'pending', -- pending, processing, completed, failed
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

## 📁 新增文件结构

```
src/
├── supabase/
│   ├── services/
│   │   ├── divination.ts       # 占卜数据服务
│   │   ├── hexagram.ts         # 卦象查询服务
│   │   └── ai.ts              # AI解读服务
│   ├── edge-functions/
│   │   └── ai-interpretation/   # AI解读边缘函数
│   │       └── index.ts
│   └── utils/
│       └── divinationUtils.ts   # 占卜工具函数
├── services/
│   └── divination.ts            # 重构后的占卜服务
├── components/
│   ├── DivinationHistory.tsx    # 占卜历史组件
│   └── AIInterpretation.tsx     # AI解读组件
└── types/
    └── divination.ts            # 更新后的占卜类型
```

## 📝 代码修改清单

### 1. 占卜服务重构
- [ ] `src/services/divination.ts` - 适配Supabase数据存储
- [ ] `src/types/divination.ts` - 更新类型定义
- [ ] `src/supabase/services/divination.ts` - 新增数据服务
- [ ] `src/supabase/services/hexagram.ts` - 新增卦象服务

### 2. 前端组件更新
- [ ] `src/pages/Divination.tsx` - 适配新的占卜流程
- [ ] `src/pages/DivinationResult.tsx` - 集成AI解读功能
- [ ] `src/components/ui/DivinationAnimation.tsx` - 保持动画，更新数据流
- [ ] `src/components/ui/LiuYaoAnimation.tsx` - 保持动画，保存结果
- [ ] `src/components/ui/MeiHuaAnimation.tsx` - 保持动画，保存结果

### 3. 新增功能组件
- [ ] `src/components/DivinationHistory.tsx` - 占卜历史记录
- [ ] `src/components/AIInterpretation.tsx` - AI解读展示
- [ ] `src/pages/Profile.tsx` - 添加占卜统计信息

### 4. 边缘函数开发
- [ ] `supabase/edge-functions/ai-interpretation/index.ts` - AI解读服务
- [ ] AI提示词优化
- [ ] 错误处理和重试机制

## 🔄 数据流设计

### 新的占卜流程
```
用户输入 → 前端占卜计算 → 动画展示 → 保存到Supabase → 调用AI边缘函数 → 更新AI解读 → 展示结果
```

### AI解读流程
```
占卜完成 → 边缘函数处理 → AI模型调用 → 结果存储 → 前端更新
```

### 历史记录流程
```
用户登录 → 查询历史记录 → 分页展示 → 详细查看 → 分享/收藏
```

## ✅ 验收标准

### 功能验收
- [ ] 六爻占卜功能完整工作
- [ ] 梅花易数功能完整工作
- [ ] 占卜结果自动保存到数据库
- [ ] AI解读功能正常工作
- [ ] 历史记录查询和展示
- [ ] 用户占卜统计信息

### 技术验收
- [ ] 所有占卜操作通过Supabase处理
- [ ] AI解读边缘函数正常响应
- [ ] 数据库RLS策略正确保护用户数据
- [ ] 前端动画效果保持原有体验
- [ ] TypeScript类型检查通过

### 性能验收
- [ ] 占卜计算响应时间 < 1秒
- [ ] AI解读响应时间 < 10秒
- [ ] 历史记录查询响应时间 < 2秒
- [ ] 动画播放流畅度保持60fps

## 🚨 技术难点和解决方案

### 1. 占卜算法保持
**问题**: 如何保持现有的占卜算法不变
**解决**: 前端保留计算逻辑，只改变数据存储方式

### 2. AI集成
**问题**: 如何集成AI解读服务
**解决**: 使用Supabase边缘函数，支持多种AI模型

### 3. 数据迁移
**问题**: 是否有现有数据需要迁移
**解决**: 如果有，制定数据迁移策略

### 4. 动画同步
**问题**: 动画播放与数据保存的同步
**解决**: 异步处理，动画完成后再保存数据

## 📊 边缘函数设计

### AI解读函数
```typescript
// supabase/edge-functions/ai-interpretation/index.ts
接口设计:
- 输入: 占卜结果、用户问题、卦象信息
- 输出: AI解读文本、解读状态
- 处理: 调用AI模型、格式化结果、错误处理
```

## 🗑️ 移除的代码/文件

### 外部API调用
- [ ] 移除对原有占卜API的依赖
- [ ] 删除相关的axios配置
- [ ] 简化占卜服务的网络请求逻辑

## 🔄 与其他阶段的衔接

### 阶段1衔接
- 认证系统已经完成，可以直接使用
- 用户表结构已建立，占卜记录可直接关联

### 阶段3准备
- AI解读功能为阶段3的高级功能奠定基础
- 数据分析功能可以使用占卜记录

## 📈 性能优化计划

### 前端优化
- [ ] 占卜动画性能优化
- [ ] 历史记录懒加载
- [ ] AI解读结果缓存

### 后端优化
- [ ] 数据库查询优化
- [ ] 边缘函数冷启动优化
- [ ] AI调用并发控制

## 📋 测试计划

### 单元测试
- [ ] 占卜计算逻辑测试
- [ ] 数据服务函数测试
- [ ] 边缘函数测试

### 集成测试
- [ ] 完整占卜流程测试
- [ ] AI解读集成测试
- [ ] 历史记录功能测试

### 用户体验测试
- [ ] 动画流畅性测试
- [ ] 响应时间测试
- [ ] 错误处理测试

---

**最后更新**: 2025-11-02
**负责人**: Claude Code
**预计完成**: 阶段2结束时
**依赖项**: Supabase配置完成