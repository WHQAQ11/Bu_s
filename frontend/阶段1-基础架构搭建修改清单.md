# 阶段1: 基础架构搭建修改清单

## 📋 总体目标
建立Supabase基础架构，迁移用户认证系统，为占卜功能迁移做好准备。

## 🔧 技术架构变更
- **认证系统**: 从JWT+localStorage → Supabase Auth
- **数据库**: 从无/外部 → Supabase PostgreSQL
- **服务层**: 从Axios外部API → Supabase客户端
- **状态管理**: 保持Zustand，适配Supabase

## 📦 依赖包更新

### 新增依赖
```json
{
  "@supabase/supabase-js": "^2.39.0",
  "@supabase/auth-ui-react": "^0.4.6",
  "@supabase/auth-ui-shared": "^0.1.8"
}
```

### 移除依赖
```json
{
  "axios": "^1.6.0"  // 如果仅用于外部API调用
}
```

## 🗃️ 数据库表设计

### 1. users 用户表
```sql
CREATE TABLE users (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  nickname TEXT,
  avatar_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2. user_stats 用户统计表
```sql
CREATE TABLE user_stats (
  user_id UUID REFERENCES users(id) ON DELETE CASCADE PRIMARY KEY,
  total_divinations INTEGER DEFAULT 0,
  last_divination_date TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

## 📁 新增文件结构

```
src/
├── supabase/
│   ├── services/
│   │   ├── auth.ts          # Supabase认证服务
│   │   └── client.ts        # Supabase客户端配置
│   ├── components/
│   │   └── AuthProvider.tsx # 认证上下文组件
│   └── types/
│       └── supabase.ts      # Supabase相关类型定义
├── services/
│   └── auth.ts              # 重构后的认证服务
├── store/
│   └── authStore.ts         # 更新后的认证状态管理
└── types/
    └── auth.ts              # 更新后的认证类型
```

## 🔧 配置文件变更

### 环境变量 (.env.local)
```env
# Supabase配置
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Vite配置更新
```typescript
// vite.config.ts - 无需重大更改
```

## 📝 代码修改清单

### 1. 认证服务重构
- [ ] `src/services/auth.ts` - 完全重写，适配Supabase Auth
- [ ] `src/types/auth.ts` - 更新类型定义
- [ ] `src/store/authStore.ts` - 重构状态管理逻辑

### 2. 新增Supabase集成
- [ ] `src/supabase/services/client.ts` - Supabase客户端配置
- [ ] `src/supabase/services/auth.ts` - Supabase认证封装
- [ ] `src/supabase/types/supabase.ts` - 类型定义
- [ ] `src/supabase/components/AuthProvider.tsx` - 认证上下文

### 3. 应用入口更新
- [ ] `src/main.tsx` - 集成Supabase认证提供者
- [ ] `src/App.tsx` - 路由认证守卫更新

### 4. 页面组件适配
- [ ] `src/pages/auth/LoginPage.tsx` - 适配新的认证API
- [ ] `src/pages/auth/RegisterPage.tsx` - 适配新的认证API
- [ ] `src/pages/Profile.tsx` - 适配新的用户数据结构
- [ ] `src/components/Layout.tsx` - 认证状态检查更新

## 🗑️ 移除的代码/文件

### 认证相关
- [ ] 删除原有的JWT token管理逻辑
- [ ] 删除localStorage中的认证状态持久化
- [ ] 简化authStore中的token相关逻辑

### API调用
- [ ] 移除对外部认证API的依赖
- [ ] 删除相关的axios拦截器配置

## ✅ 验收标准

### 功能验收
- [ ] 用户可以通过邮箱注册新账户
- [ ] 用户可以登录现有账户
- [ ] 登录状态在页面刷新后保持
- [ ] 用户可以安全登出
- [ ] 未登录用户被正确重定向到登录页

### 技术验收
- [ ] 所有认证操作通过Supabase处理
- [ ] 认证状态在Zustand中正确管理
- [ ] TypeScript类型检查通过
- [ ] 应用构建无错误
- [ ] 开发服务器正常启动

### 安全验收
- [ ] RLS策略正确配置，用户只能访问自己的数据
- [ ] 认证令牌由Supabase自动管理
- [ ] 敏感信息不在前端暴露

## 🚨 注意事项

1. **数据迁移**: 如果有现有用户数据，需要制定迁移策略
2. **向后兼容**: 确保现有UI组件在新的认证系统下正常工作
3. **错误处理**: Supabase错误码需要适配到现有的错误处理系统
4. **用户体验**: 认证流程的UI/UX应保持一致

## 📊 进度跟踪

### 已完成任务 ✅
- [x] 架构设计和技术选型

### 进行中任务 🔄
- [ ] 环境配置和依赖安装

### 待完成任务 ⏳
- [ ] 数据库表创建
- [ ] 认证服务实现
- [ ] 前端适配和测试

## 🔄 与其他阶段的衔接

### 阶段2准备
- 本阶段完成后，用户系统完全基于Supabase
- 为阶段2的占卜数据存储奠定基础
- 认证API结构为占卜功能提供参考

### 风险控制
- 每个功能模块完成后立即测试
- 保持现有功能的向后兼容
- 分步骤迁移，避免大规模破坏性变更

---

**最后更新**: 2025-11-02
**负责人**: Claude Code
**预计完成**: 阶段1结束时